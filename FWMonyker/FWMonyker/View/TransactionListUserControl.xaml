<UserControl x:Class="FWMonyker.View.TransactionListUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:View="clr-namespace:FWMonyker.View"
             xmlns:Model="clr-namespace:FWMonyker.Model;assembly=FWMonyker.Model"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="500
">
    <UserControl.Resources>
        <DataTemplate DataType="{x:Type Model:Transaction}">
            <View:TransactionUserControl/>
        </DataTemplate>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="130"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel>
            <TextBlock Margin="0 0 0 25" Style="{StaticResource textHeader}">
            transactions
            </TextBlock>
            <Grid Height="50">
                <Grid.RowDefinitions>
                    <RowDefinition Height="1*"/>
                    <RowDefinition Height="1*"/>
                </Grid.RowDefinitions>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Label Content="Sortér efter dato"/>
                    <Button Command="{Binding SortTransactionsC}" FontFamily="Marlett" FontSize="10" Content="v" Width="15" Height="20"/>
                </StackPanel>


                <Button Content="tilføj" HorizontalAlignment="Left" VerticalAlignment="Top" Width="40" Height="25" Grid.Row="0"
                    Command="{Binding DataContext.MainViewodel, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}" 
                    CommandParameter="{Binding This}"/>

                <TextBox HorizontalAlignment="Left" Height="25" Grid.Row="1" TextWrapping="Wrap" Text="{Binding SearchBox, UpdateSourceTrigger=PropertyChanged}"  VerticalAlignment="Top" Width="500">
                    <TextBox.InputBindings>
                        <KeyBinding Command="{Binding Search}" Key="Enter"/>
                    </TextBox.InputBindings>
                </TextBox>

            </Grid>
        </StackPanel>

        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <ItemsControl Margin="3"
                      ItemsSource="{Binding Transactions}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Name="ContentStack"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <View:TransactionUserControl/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
                <ItemsControl.ItemContainerStyle>
                    <Style>
                        <Setter Property="Control.FontSize" Value="13"/>
                        <Setter Property="Control.FontFamily" Value="Helvetica"/>
                        <Setter Property="Control.FontWeight" Value="Light"/>
                    </Style>
                </ItemsControl.ItemContainerStyle>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
